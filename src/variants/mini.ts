import { BoardConfig } from '../types/sudoku';

export const mini4Config: BoardConfig = {
  variant: 'mini4',
  size: 4,
  boxWidth: 2,
  boxHeight: 2,
};

export const mini6Config: BoardConfig = {
  variant: 'mini6',
  size: 6,
  boxWidth: 3,
  boxHeight: 2,
};
